# Maintaining This Profile

This repository powers the GitHub profile page for `shockley6668`.

To update the page, edit `README.md`, then commit and push:

```bash
git add README.md MAINTAINING.md
git commit -m "Update profile README"
git push
```

For GitHub to display this README on the profile page, the remote repository must be public and named exactly:

```text
shockley6668/shockley6668
```
